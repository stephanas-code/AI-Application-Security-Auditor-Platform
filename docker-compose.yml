services:
  appsec-platform:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: appsec_auditor_platform
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    volumes:
      - ./scans:/tmp/appsec_scans
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: unless-stopped
